services:
  api:
    build:
      context: ./api
    container_name: astro-kovesh-api
    env_file:
      - ./api/.env
    environment:
      APP_ENV: production
    restart: unless-stopped
    ports:
      - "8010:8010"
